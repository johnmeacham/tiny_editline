Builds both an arduino and a native version of example code.

The arduino version is for an arduino uno and opens up the serial port at
38400 baud. You should be able to connect to it from any serial termial
program.

The native version ends up in .pio/build/native/program
